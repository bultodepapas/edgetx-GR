# Generated by dev/preview.lua and dev/audit-preview.lua - regenerate, do not
# edit or commit.
dev/preview.html
dev/audit-preview.html

# Generated by dev/shots.lua - individual render shots (SVG + rasterised PNG).
dev/shots/

# NOT ignored, deliberately: docs/ holds the official option collage
# (dev/collage.lua). The README embeds it, so it has to survive a clone -
# unlike dev/shots/, which is scratch for close-up review.

# Written to the SD card by the firmware on first load (loadScript "bt").
*.luac
